#include <stdio.h>
#include <sys/time.h>
#include <sys/types.h>
#include <stdint.h>
#include "../queue/queue.h"

int main(void)
{
  Queue *watingQ = createQueue(10);
  Queue *arrivedQ = createQueue(10);

  
  return 0;
}